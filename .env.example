# Local development — copy to .env.local and fill in your key
# The VITE_ prefix exposes the value to the browser bundle (dev only).
# In production, set ANTHROPIC_KEY in Vercel environment variables instead.
VITE_ANTHROPIC_KEY=sk-ant-...
